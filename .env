# Required
SMTP_HOST=smtp.zoho.com
SMTP_USER=ceo@mrkirof.com
# SMTP_PASS="Rj4UKJJn31Fv"   # Gmail: use an App Password, not your real password
SMTP_PASS="xgVfysV8zpmy"   # Gmail: use an App Password, not your real password

# Optional (defaults shown)
SMTP_PORT=587
SMTP_SECURE=false             # true for port 465 (SSL), false for 587 (STARTTLS)
SMTP_FROM_NAME=MrKirof
SMTP_FROM_EMAIL=ceo@mrkirof.com              # Falls back to SMTP_USER if unset

#server
SERVER_PORT=5000

# client
CLIENT_URL=http://localhost:8080

#CONTACT_TO_EMAIL
CONTACT_TO_EMAIL=ceo@mrkirof.com
# ── Provider quick reference ──────────────────────────────────────────────────
#
# Gmail          smtp.gmail.com          port 587  secure false
# SendGrid       smtp.sendgrid.net       port 587  secure false  user=apikey
# Mailgun        smtp.mailgun.org        port 587  secure false
# Resend         smtp.resend.com         port 465  secure true   user=resend
# Brevo          smtp-relay.brevo.com    port 587  secure false
# Postmark       smtp.postmarkapp.com    port 587  secure false
# Mailtrap (dev) sandbox.smtp.mailtrap.io port 2525 secure false
